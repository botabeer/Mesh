
# render.yaml template - edit before use. Render's manifest requires custom entries.
# Example (manual steps often needed):
# - Create a Managed Redis instance on Render and set REDIS_URL in your service environment variables.
# Use the provided .env file and docker-compose for local testing.
